.inner-area
  %h1= "Cancelar servicio #{@requested_service.number}"
  = form_for @requested_service, :remote => true, :html => {:'data-type' => 'html', :id => 'cancel-sample-form'} do |f|
    = f.hidden_field :status, {:value => RequestedService::CANCELED}
    #field_service_request_sample_description
      = label_tag 'activity_log_msg', 'Razones:'
      = text_area_tag 'activity_log_msg'
    .clearfix
    #dialog-submit-button-area
      = f.submit "Cancelar Servicio"

