%ul
  - @users.each do |user|
    %li.user-item.item{"data-id" => user.id}
      .user-item-name{:id => "user_name_#{user.id}"}= user.full_name
      .user-item-access= user.access_text
.items-counter
  .items-counter-inner
    = "#{@users.count} consumibles"